# ICE-ZERO 모델 기획서

## 1. 목표

ICE-ZERO는 다음 네 단계를 분리해 구현한다.

1. AI가 3시간 후 노면온도를 예측한다.
2. 예측 노면온도와 수분·건물 그림자로 도로의 상대 제설 위험도를 계산한다.
3. Hybrid Budgeted Maximum Coverage/Knapsack으로 예산 안에서 처리할 도로를 정한다.
4. 도로 그래프 기반 VRP 휴리스틱으로 차량별 작업 순서와 이동 경로를 만든다.

AI 모델과 최적화 알고리즘의 역할은 다르다. MLP는 노면온도를 예측하고, BMC/Knapsack과 VRP는 각각 처리 대상과 작업 순서를 결정한다.

## 2. 현재 AI 모델

### 2.1 학습 목표

- 모델: PyTorch Lightning residual MLP
- 모델 버전: `road_surface_temperature_residual_mlp_v2`
- 실제 정답: 기상청 도로기상관측 원지(도) `노면온도(°C)`
- 예측 시점: 입력 시각에서 3시간 후
- pseudo-label: 사용하지 않음
- 사고확률 MLP: 사용하지 않음

실제 도로별·시각별 사고 정답이 없으므로 사고확률을 학습했다고 주장하지 않는다. 현재 MLP의 검증 범위는 강남 중심에서 약 5.25km 떨어진 원지(도) 단일 관측소의 실측 노면온도와 0°C 이하 관측 사건이다.

### 2.2 입력 피처

MLP는 다음 26개 피처를 사용한다.

| 구분 | 피처 |
|---|---|
| 온도 | `temp`, `ground_temp_now`, `ground_temp_lag_1h`, `temp_6h_mean` |
| 대기 | `humidity`, `dewpoint_depression`, `wind` |
| 수분·적설 | `precip_6h`, `snow`, `new_snow_6h` |
| 일사 | `solar`, `solar_3h_sum`, `sunshine` |
| 시간 | `hour_sin`, `hour_cos`, `day_sin`, `day_cos` |
| 노면 | `road_surface_temp_now`, 1·2·3·6시간 lag, 3·6시간 이동평균, 1·3시간 변화율 |

`shadow_index`, `pop_weight`, `LENGTH`, `LANES`, `area`, `deicing_cost`, 기존 계산식 `risk`는 학습 피처에서 제외한다.

### 2.3 네트워크 구조

```text
26 features
  -> Linear(128) -> LayerNorm -> SiLU -> Dropout(0.08)
  -> Linear(128) -> LayerNorm -> SiLU -> Dropout(0.08)
  -> Linear(64)  -> LayerNorm -> SiLU -> Dropout(0.08)
  -> Linear(1)
```

출력은 현재 노면온도에 대한 3시간 후 변화량이다.

```text
predicted_road_surface_temp = road_surface_temp_now + predicted_delta
```

- 손실: Smooth L1 loss
- 최적화: AdamW
- 최대 epoch: 180
- batch size: 256
- learning rate: 0.001
- weight decay: 0.0001
- 조기 종료 patience: 24
- GPU: CUDA 사용 가능 시 자동 사용

## 3. 시간 분할과 누수 방지

| 용도 | 기간 |
|---|---|
| 개발 학습 | 2024-11~2025-02 |
| 조기 종료·동결확률 보정 | 2025-11 |
| 최종 시간 홀드아웃 | 2025-12 |

2025년 12월은 학습, epoch 선택, 동결확률 보정에 사용하지 않는다. 입력과 정답은 행 순서로 shift하지 않고 `issue_time + 3시간 = valid_time`을 정확히 조인한다.

결측 기상 피처는 같은 연속 관측 구간에서 최대 6시간의 과거값으로만 보완한다. 미래 관측값을 과거 입력으로 역전파하는 보간과 역방향 채우기는 사용하지 않는다.

## 4. 평가

회귀 지표:

- RMSE
- MAE
- R²
- 평균 편향
- 현재 노면온도 지속 기준 대비 RMSE skill
- 95% bootstrap 구간
- 월별 및 관측 -3~+3°C 구간 성능

0°C 이하 관측 사건 지표:

- Precision
- Recall
- F1
- PR-AUC
- ROC-AUC
- Brier score

현재 수치는 `outputs/reports/road_surface_model_metrics.json`을 기준으로 한다. 미학습 2025년 12월 724시간에서 RMSE 1.064°C, MAE 0.804°C, R² 0.946이었고 현재 노면온도 지속 기준보다 RMSE가 61.2% 낮았다. 0°C 이하 사건은 Precision 0.958, Recall 0.806, F1 0.875, PR-AUC 0.976이었다. 이 수치를 강남 전체 도로별 결빙 정확도나 사고 발생확률 정확도로 표현하지 않는다.

## 5. 도로 상대 위험도

지도와 공간 검증에는 미학습 2025년 12월의 MLP 예측만 사용한다. 운영용 모델은 전체 관측으로 별도 재학습해 모델 파일에 함께 저장하지만, 과거 성능 검증 지도에는 사용하지 않는다.

도로 위험도 변환 순서:

1. 3시간 후 노면온도 예측
2. 강수, 적설, 신적설, 습도·이슬점 차이로 수분 지수 계산
3. 건물과 태양 위치로 계산한 `shadow_index`로 유효 노면온도 보정
4. 2025년 11월 관측으로 보정한 0°C 이하 확률 적용
5. 결빙 후보 사건의 평균, 90백분위, 99백분위를 결합

`risk`는 절대 결빙 확률이 아니라 도로 간 상대 제설 우선순위 지수다.

## 6. 제설 우선순위와 비용

```text
priority_score = risk * (0.7 + 0.3 * pop_weight)
```

생활인구는 사람이 많이 노출되는 도로를 우선 처리하기 위한 가중치다. 제설 비용은 학습 피처가 아니며 `config/deicing_costs.json`에서 관리한다.

```text
deicing_kg = road_length * road_width * spread_rate
deicing_cost = deicing_kg * (material_cost + environmental_cost)
             + road_length_km * labor_cost_per_km
```

## 7. 대상 선정과 경로

### 7.1 Hybrid BMC/Knapsack

- Knapsack 요소: 도로 위험도와 생활인구 대비 비용 효율
- BMC 요소: 250m 격자에서 새로 커버되는 위험·인구 영역
- 목적: 같은 지역의 중복 선택을 줄이고 제한 예산의 공간 커버를 높임

### 7.2 VRP 휴리스틱

- 선택 도로의 연결요소와 작업량으로 필요한 차량 수를 결정
- 외곽 도로 시작점을 우선 사용
- 표준 노드·링크의 방향 그래프로 이동 경로 생성
- 작업 도로와 단순 이동 도로를 구분
- 도로가 연결되지 않은 경우 직선 순간이동 선을 만들지 않고 연결 실패로 기록

## 8. 독립 공간 확인

`data/raw/weather/13_24_freezing.csv`의 강남구 결빙사고 다발지역은 학습과 계수 조정에 사용하지 않는다.

- 여러 연도에 반복된 동일 장소를 공간 군집으로 통합
- 사고지역과 겹치는 모든 도로의 평균 위험 백분위를 사용
- 지역 내부의 최고 위험 도로만 고르는 방식은 사용하지 않음
- 무작위 비교도 각 사고지역의 도로 개수를 동일하게 맞춤
- 제안 경로 평가는 사고지역 내부 도로 중 선택된 비율로 비교

이 검증은 실제 사고 다발지역과 상대 위험 순위의 공간적 일치도다. 도로 결빙 분류 정확도나 사고 감소율 실증이 아니다.

행정안전부 상습결빙구간 3,358건도 자동 수집하며, 서울 268건 중 강남구 7개 구간을 표준 링크 28개와 연결한다. 이 자료를 학습과 계수 조정에 사용하지 않고 두 번째 독립 공간 검증으로 적용한 결과 평균 위험 백분위는 0.400, 무작위 평균은 0.501, p-value는 0.896이었다. 즉 단일 인접 노면 관측을 추가한 현재 모델도 공식 상습결빙구간에 일반화되지 않았으며, 교량·경사·배수·포장재와 도로별 노면센서 보강이 필요하다.

## 9. 모델 파일 검증

- 모델: `models/road_surface_temperature_mlp.pt`
- manifest: `models/road_surface_model_manifest.json`
- 보고서: `outputs/reports/road_surface_model_metrics.json`
- 모델 카드: `outputs/reports/road_surface_model_card.json`

`main_pipeline.py`는 모델을 읽기 전에 다음을 확인한다.

1. 모델 파일과 manifest 존재
2. SHA-256 일치
3. 모델 버전 일치
4. 26개 입력 피처와 순서 일치
5. 필수 state dict와 scaler 존재

모델 파일은 약 230KB이므로 Git에서 제외하지 않는다.

## 10. 실행

```powershell
python scripts\preprocess_data.py
python scripts\train_mlp_pipeline.py
python scripts\validation_simulation.py
python -m pytest tests --basetemp .pytest_run
```

학습 없이 저장 모델로 지도와 경로만 다시 만들 때:

```powershell
python scripts\main_pipeline.py
```

## 11. 현재 한계

1. 노면온도 정답은 강남 인접 원지(도) 단일 관측소이며 강남구 도로별 센서가 아니다.
2. 실측 노면온도는 확보했지만 도로별·시각별 결빙 상태 정답은 없다.
3. 사고 다발지역은 장기간 누적 공간 proxy이며 특정 시각의 결빙 정답이 아니다.
4. 건물 그림자에 가로수, 지형, 교량 구조물은 포함되지 않는다.
5. 비용 단가는 설정값이며 실제 지자체 계약·작업일지로 교체해야 한다.
6. VRP는 휴리스틱이며 전역 최적해를 보장하지 않는다.

## 12. 다음 단계

우선순위는 다음과 같다.

1. `LINK_ID, timestamp, road_surface_temp, icing` 형식의 강남 도로별 센서·현장 라벨 확보
2. 원지 외 강남구 인근 다지점 도로기상 또는 초단기 예보 연동
3. 도로 단위 공간 홀드아웃과 시간 홀드아웃을 함께 수행
4. 실제 제설 작업 전후 노면상태와 사고·민원 기록으로 효과 검증
5. 충분한 실제 라벨이 확보된 뒤 GNN, LSTM, Transformer와 비교

현재 단계에서는 복잡한 모델을 추가하는 것보다 정답 데이터의 공간 대표성과 독립 검증을 강화하는 것이 더 중요하다.
