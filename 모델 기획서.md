# ICE-ZERO 모델 기획서

## 1. 목표

겨울철 도로의 3시간 후 노면온도를 예측하고, 결빙 위험이 높은 도로를 선별해 제한된 예산과 차량으로 제설 순서와 이동 경로를 생성한다.

## 2. 전체 구조

```text
기상·노면 관측 데이터
        ↓
Residual MLP 노면온도 예측
        ↓
수분·적설·건물 그림자를 결합한 상대 위험도 산정
        ↓
BMC/Knapsack 기반 제설 대상 선정
        ↓
도로 그래프 기반 VRP 차량 경로 생성
```

- **MLP**: 3시간 후 노면온도 예측
- **위험도 계산**: 예측 노면온도에 강수, 적설, 습도, 그림자 조건 반영
- **BMC/Knapsack**: 예산 안에서 처리할 도로 결정
- **VRP**: 선택된 도로의 차량별 작업 순서와 이동 경로 결정

## 3. AI 모델

- 모델: PyTorch Lightning Residual MLP
- 버전: `road_surface_temperature_residual_mlp_v2`
- 입력: 기온·지면온도·습도·강수·적설·일사·시간·과거 노면온도 등 26개 피처
- 출력: 3시간 후 노면온도(°C)
- 정답 데이터: 기상청 도로기상관측 원지(도) 실측 노면온도
- 학습 방식: 현재 노면온도에서 3시간 후 변화량을 예측하는 회귀 모델
- pseudo-label 및 사고확률 계산식: 사용하지 않음

## 4. 검증 결과

학습과 보정에 사용하지 않은 2025년 12월 724시간을 최종 평가에 사용했다.

| 지표 | 결과 |
|---|---:|
| RMSE | 1.064°C |
| MAE | 0.804°C |
| R² | 0.946 |
| 지속 예측 대비 RMSE 개선 | 61.2% |
| 0°C 이하 Precision | 0.958 |
| 0°C 이하 Recall | 0.806 |
| 0°C 이하 F1-score | 0.875 |
| PR-AUC | 0.976 |

이 결과는 **원지(도) 단일 관측소의 노면온도 및 0°C 이하 사건 예측 성능**이다. 강남구 전체 도로의 결빙 정확도나 사고 발생확률로 해석하지 않는다.

## 5. 제설 의사결정

도로별 상대 위험도와 생활인구를 이용해 우선순위를 정하고, 비용과 공간 커버리지를 함께 고려해 처리 대상을 선정한다. 이후 표준 노드·링크 도로망을 따라 차량별 작업 경로를 생성하며, 작업 도로와 단순 이동 도로를 구분한다.

## 6. 현재 한계와 개선 방향

- 노면온도 정답이 강남 인접 단일 관측소이므로 도로별 공간 차이를 직접 학습하지 못한다.
- 실제 도로별 결빙 상태와 사고 시점 라벨이 없어 위험도는 절대 확률이 아닌 상대 우선순위다.
- 교량, 경사, 배수, 포장재와 강남구 도로별 노면센서 데이터를 추가해야 한다.
- 실제 제설 작업 전후의 노면 상태와 사고·민원 기록으로 현장 효과를 검증해야 한다.

현재 단계에서는 모델을 더 복잡하게 만드는 것보다 도로별 실측 데이터와 독립 검증 자료를 확보하는 것이 우선이다.
