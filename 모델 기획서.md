# 모델 기획서

## 1) 작품 방향 요약
노션 기획의 핵심은 **AI를 활용한 염화칼슘 난사 방지 및 효율적 제설 맵핑 시스템**이다.

목표는 단순히 결빙 여부를 맞히는 것이 아니라, 아래 요소를 함께 고려해 **최소 비용으로 최대 안전 효과**를 얻는 것이다.

- 도로 결빙 위험 예측
- 사고 확률 예측으로 제설 경로/대상 선정 결과 검증
- 건물 그림자, 지열, 바람, 기온, 강수 등 결빙 영향 요소 반영
- 사람/차량/자전거 등 유동성 기반 위험 가중치 반영
- 염화칼슘 비용, 인건비, 환경 비용, 민원/사고 위험 비용 최소화
- Unity 기반 50x50 grid 시뮬레이션으로 데이터 부족 문제 보완

현재 `src` 구현은 이 전체 기획 중 **결빙 위험 예측 모델**, **사고 확률 예측 모델**, **도메인 데이터 모델**, **배차/작업 최적화 알고리즘**의 1차 MVP에 해당한다.

---

## 2) 현재 구현 확인

### 2.1 결빙 위험 예측 모델
- 파일: `src/torch_lightning_risk_model.py`
- 구현:
  - `IcingRiskNet`: MLP 기반 이진 위험 예측 모델
  - 구조: 설정 가능한 `hidden_dims` 기반 `Linear -> BatchNorm -> ReLU -> Dropout` 반복 구조
  - 출력: 도로 세그먼트 또는 grid cell별 결빙 확률 `0~1`
  - `IcingRiskLightningModule`: PyTorch Lightning 학습/검증/테스트 루프 제공
- 손실 함수:
  - `binary_cross_entropy_with_logits`
  - `pos_weight`를 지원해 결빙 샘플 부족 문제에 일부 대응 가능
- 현재 지표:
  - accuracy
  - precision
  - recall
  - F1
  - balanced accuracy
- 추론 보조:
  - `predict_logits`: calibration 또는 loss 계산용 raw logit 반환
  - `predict_labels`: 설정 가능한 threshold 기반 위험/비위험 라벨 반환

현재 모델은 **초기 베이스라인으로는 적절**하다. 다만 노션 기획처럼 공간 구조, 그림자, 유동 인구, 도로 연결성을 반영하려면 MLP만으로는 한계가 있다.

### 2.1.1 사고 확률 예측 모델
- 파일: `src/torch_lightning_risk_model.py`
- 구현:
  - `AccidentRiskLightningModule`: 겨울철 사고 발생 확률을 예측하는 MLP 이진 분류기
  - 구조: `IcingRiskLightningModule`과 같은 `IcingRiskNet` 기반 tabular MLP
  - 출력: 도로 세그먼트별 사고 확률 `0~1`
- 역할:
  - 결빙 예측만으로는 “경로 최적화가 AI인가?”라는 질문에 약할 수 있으므로, 사고 확률 예측 AI를 별도로 둔다
  - `Knapsack/BMC/VRP`는 제설 대상과 순서를 정하는 최적화/휴리스틱이고, AI는 위험도와 사고확률을 예측하는 부분이라고 설명할 수 있다
  - 사고 확률이 높은 구간을 얼마나 많이 커버했는지로 제설 경로 결과를 검증한다

발표에서는 아래처럼 분리해서 말하는 것이 좋다.

```text
AI 모델: 결빙 위험도 예측 + 사고 확률 예측
최적화 알고리즘: 예산 안에서 어디를 제설할지 선택하고, 차량이 어떤 순서로 돌지 계산
검증 지표: 선택된 경로가 고위험/고사고확률 구간을 얼마나 커버했는지
```

### 2.1.2 위험도 산식 보조 모듈
- 파일: `src/risk_scoring.py`
- 역할:
  - 실제 결빙 라벨이 부족한 초기 단계에서 pseudo-risk score 생성
  - 실제 사고 라벨이 부족한 초기 단계에서 pseudo accident probability 생성
  - 기상, 도로, 그림자, 유동성 정보를 결합해 `0~1` 위험도 계산
  - 결빙 위험도, 사고 확률, 노출 점수를 결합해 `priority_score`와 `safety_gain_score` 계산
  - 도로 길이/폭 기반 제설 비용 산정
  - `DeicingCandidate` 생성으로 BMC/VRP 파이프라인에 연결
- 참고한 방향:
  - 다운받은 참고 프로젝트의 `위험도 -> 우선순위 -> 비용 -> 최적화` 흐름을 사용
  - 단, 현재 프로젝트에서는 MLP를 유지하고, `risk_scoring.py`는 학습 전 보조 산식 및 baseline 생성기로 둔다

이 구조를 쓰면 실제 라벨이 없을 때는 pseudo-risk/pseudo-accident로 데모를 만들고, 라벨이 생기면 MLP 모델 출력으로 교체할 수 있다.

### 2.2 도메인 데이터 모델
- 파일: `src/deicing_models.py`
- 주요 모델:
  - `WeatherSnapshot`: 기온, 노면온도, 이슬점, 습도, 풍속, 강수 정보
  - `RoadSegment`: 도로 길이, 차선 수, 제한속도, 경사, 교량 여부, 좌표
  - `GridCell`: Unity 50x50 grid 또는 지도 cell 단위 공간 모델
  - `UrbanExposure`: 건물 그림자, 일조 시간, 지열/열섬 지수
  - `MobilitySnapshot`: 보행자, 차량, 자전거 유동량 및 노출 점수
  - `RoadCondition`: 노면 상태, 심각도, 마찰계수, 적설/결빙 두께
  - `RiskPrediction`: 결빙 확률, 사고 확률, 예측 노면 상태, 심각도, 노출 점수, 안전 개선 점수, 권장 살포재/살포량
  - `AccidentPrediction`: 사고 확률, 결빙 확률, 노출 점수, 안전 개선 점수, 모델 버전
  - `DeicingOperation`: 실제 제설 작업 지시 및 실행 결과
  - `SmartDeicingMapModel`: 전체 데이터를 묶는 상위 모델

이 구조는 기획서의 “AI 결과 기반 제설 실행”을 담기에 좋다. 특히 `GridCell`, `UrbanExposure`, `MobilitySnapshot`를 통해 노션의 `50x50 grid`, `그림자 지수`, `유동 인구` 개념을 데이터 모델에 반영했다. 환경 비용은 아직 최적화 비용 함수 단계에서 확장해야 한다.

### 2.3 제설 대상 선정 및 경로 최적화
- 파일: `src/dispatch_planner.py`
- 구현:
  - `DeicingCandidate`: 제설 후보 도로/격자 묶음, 비용, 가치, 커버 grid cell, 위치, 작업 시간
  - `knapsack_select`: 제한 예산 안에서 가치가 큰 제설 후보를 고르는 기본 베이스라인
  - `budgeted_maximum_coverage_select`: 위험 grid cell 중복을 줄이며 최대 커버리지를 얻는 최종 선정 알고리즘
  - `hybrid_bmc_knapsack_select`: 새 커버리지 증가량과 도로 자체 우선순위를 함께 보는 주력 선정 알고리즘
  - `summarize_selection`: 선택된 후보의 비용, 가치, 커버 cell 요약
  - `RoutingVehicle`: 차량 용량, 작업 가능 시간, 출발/복귀 위치
  - `plan_vrp_routes`: 선택된 후보들을 차량별 순서로 배치하는 value-biased VRP형 nearest-neighbor 휴리스틱
  - `plan_deicing_targets_and_routes`: BMC로 대상 선정 후 VRP로 경로화하는 통합 함수
  - `Job`: 작업 수요와 우선순위
  - `Vehicle`: 차량별 처리 가능 용량
  - `Allocation`: 차량-작업 할당 결과
  - `greedy_allocate`: 우선순위 높은 작업부터 빠르게 배정하는 베이스라인
- 현재 주력 흐름:
  - `MLP 또는 pseudo-risk로 결빙 위험도 산정`
  - `AccidentRiskLightningModule 또는 pseudo-accident로 사고 확률 산정`
  - `priority_score, safety_gain_score, deicing_cost 계산`
  - `DeicingCandidate 생성`
  - `Hybrid BMC/Knapsack으로 어디를 처리할지 선정`
  - `VRP 휴리스틱으로 차량별 처리 순서 생성`

현재 구현은 노션 기획에 맞춰 **AI 예측, 선정 문제, 경로 문제를 분리**한다. MLP가 결빙 위험도와 사고 확률을 만들고, `Hybrid BMC/Knapsack`이 “어디를 제설할지”를 정하고, `VRP`가 “차량이 어떤 순서로 돌지”를 정한다.

---

## 3) 노션 기획 기준 보강 모델

### 3.1 입력 feature 설계
위험 예측 모델의 입력은 다음처럼 확장한다.

| 구분 | feature 예시 | 현재 반영 상태 |
| --- | --- | --- |
| 기상 | 기온, 노면온도, 이슬점, 습도, 강수량, 풍속, 돌풍 | `WeatherSnapshot`에 대부분 존재 |
| 도로 | 경사, 교량 여부, 차선 수, 길이, 제한속도 | `RoadSegment`에 존재 |
| 노면 | 마찰계수, 적설량, 결빙 두께, 노면 상태 | `RoadCondition`에 존재 |
| 도시 구조 | 건물 그림자 지수, 일조량, 지열/열섬 지수 | `UrbanExposure`에 존재 |
| 유동성 | 보행자, 차량, 자전거 통행량 | `MobilitySnapshot`에 존재 |
| 운영 이력 | 최근 살포량, 살포재 종류, 최근 제설 시간 | `DeicingOperation` 기반 확장 가능 |
| 사고 이력 | 겨울철 사고 건수, 사고 유형, 노면 상태별 사고 통계 | `AccidentPrediction`과 사고확률 MLP로 확장 |

### 3.2 라벨 정의
학습 라벨은 반드시 명확히 정의해야 한다.

- 결빙 라벨: `target_time` 시점에 해당 도로 블록이 결빙 상태이면 `1`, 아니면 `0`
- 결빙 판정 기준 예시:
  - `surface_state == ICE`
  - 또는 `ice_thickness_mm > 0`
  - 또는 `friction_coefficient`가 임계값 이하
- 사고 라벨: `target_time` 또는 특정 겨울 기간 안에 해당 도로/격자에서 사고가 발생했으면 `1`, 아니면 `0`
- 사고 판정 기준 예시:
  - 겨울철 교통사고 이력 데이터의 사고 발생 여부
  - 노면 상태가 `결빙`, `적설`, `젖음`인 사고만 별도 필터링
  - 실제 사고 위치가 없으면 행정동/격자 단위 사고율을 도로 세그먼트에 매핑
- 예측 horizon:
  - MVP: 1시간 뒤 결빙 위험
  - 사고확률: 현재 기상/도로/유동성 조건에서 단기 사고 위험
  - 확장: 30분, 1시간, 3시간 등 다중 horizon 예측

### 3.3 모델 단계
현재 구현과 최종 기획을 나누면 자연스럽다.

1. **MVP 모델**
   - 현재 `IcingRiskNet` 사용
   - 같은 MLP 구조의 `AccidentRiskLightningModule` 사용
   - 도로 세그먼트 단위 tabular feature 입력
   - 결빙 확률과 사고 확률을 따로 예측해 최적화 입력으로 사용
   - 빠르게 학습/검증 가능한 baseline

2. **공간 grid 모델**
   - Unity 50x50 grid 또는 지도 grid cell을 입력 단위로 사용
   - 그림자 지수, 지형, 도로 타입, 기상 조건을 cell feature로 변환
   - CNN 또는 간단한 U-Net 계열로 위험 heatmap 예측 가능

3. **도로 그래프 모델**
   - 도로 segment를 node, 연결 관계를 edge로 구성
   - GNN(Graph Neural Network)으로 인접 도로의 결빙 전파/연결성 반영
   - 노션의 “위치 간 연결성”, “흐름 기반 최적화”와 가장 잘 맞음

---

## 4) 최적화 모델 구성

### 4.1 1단계: Knapsack 베이스라인
`knapsack_select`는 각 제설 후보를 하나의 아이템으로 보고, 제한된 예산 안에서 가치 합이 최대가 되도록 선택한다.

- 장점:
  - 설명이 쉽고 발표용 baseline으로 좋음
  - “위험도 높은 도로를 예산 안에서 선택한다”는 구조가 직관적
  - Budgeted Maximum Coverage 결과와 비교 가능
- 한계:
  - 후보끼리 커버하는 위험 grid cell이 겹쳐도 중복 점수로 계산될 수 있음
  - 차량 이동 경로는 반영하지 않음

### 4.2 2단계: Budgeted Maximum Coverage
`budgeted_maximum_coverage_select`는 각 제설 후보가 커버하는 위험 grid cell 집합을 보고, 제한 예산 안에서 새롭게 덮는 위험 cell 가치가 가장 큰 후보를 고른다.

이 프로젝트에서 BMC가 잘 맞는 이유:

- 노션 기획의 `50x50 grid`와 자연스럽게 연결됨
- 결빙 위험 heatmap에서 고위험 cell을 최대한 많이 커버할 수 있음
- 인접 도로 후보끼리 같은 위험 구역을 중복 처리하는 문제를 줄임
- “염화칼슘 난사 방지”라는 주제를 알고리즘적으로 설명하기 좋음

현재 주력 구현은 순수 BMC보다 한 단계 확장한 `hybrid_bmc_knapsack_select`다. 이 함수는 다운로드 프로젝트의 접근처럼 아래 두 값을 함께 본다.

```text
hybrid_value =
  coverage_weight * normalized_new_coverage
  + priority_weight * normalized_candidate_priority
```

즉, 새 위험 격자를 많이 덮는 후보를 선호하되, 도로 자체의 위험도/유동성 우선순위도 버리지 않는다.

선정 점수 예시:

```text
cell_value =
  (0.55 * icing_probability + 0.45 * accident_probability)
  * exposure_weight
  * severity_weight
```

후보 비용 예시:

```text
candidate_cost =
  material_cost
  + labor_cost
  + environmental_cost
  + estimated_service_time_cost
```

### 4.3 3단계: VRP 경로화
`plan_vrp_routes`는 BMC로 선택된 제설 후보들을 차량별 처리 순서로 바꾼다.

- 입력:
  - 선택된 `DeicingCandidate`
  - 차량별 `capacity`
  - 차량별 `max_time`
  - 출발/복귀 위치
- 출력:
  - 차량별 방문 순서
  - 총 이동 거리
  - 총 작업 시간
  - 사용 용량

현재 구현은 MVP용 nearest-neighbor 휴리스틱이다. 즉, 완전 최적 VRP solver는 아니지만 발표/프로토타입에서 “선정 후 경로화” 흐름을 보여주기에는 충분하다.

다운받은 프로젝트는 KMeans로 차량 구역을 나눈 뒤 priority-biased nearest neighbor를 적용한다. 현재 프로젝트는 의존성을 늘리지 않기 위해 KMeans는 넣지 않았고, 대신 거리와 후보 가치를 함께 보는 `value_weight` 기반 nearest neighbor를 적용했다. 추후 `scikit-learn`을 사용할 수 있으면 KMeans 군집 분배를 추가할 수 있다.

비용 함수 예시:

```text
effective_cost =
  material_cost
  + labor_cost
  + travel_time_cost
  + environmental_cost
  - safety_gain
```

여기서 `safety_gain`은 다음처럼 계산할 수 있다.

```text
safety_gain =
  (0.45 * icing_probability + 0.55 * accident_probability)
  * exposure_weight
  * severity_weight
```

---

## 5) 현재 코드에서 주의할 점

### 5.1 priority 방향 통일 필요
`src/deicing_models.py`의 `DeicingZone.service_priority`는 **1이 최고 우선순위**다.

반면 `src/dispatch_planner.py`의 `Job.priority`는 현재 테스트와 정렬 기준상 **숫자가 클수록 우선순위가 높다**.

따라서 실제 연동 시 아래 중 하나로 통일해야 한다.

- 방법 A: 배차 `Job.priority`도 1이 최고가 되도록 수정
- 방법 B: 도메인 모델에서 배차 작업 생성 시 `dispatch_priority = 6 - service_priority`처럼 변환

MVP에서는 방법 B가 안전하다. 기존 테스트와 구현을 깨지 않으면서 도메인 모델의 의미를 유지할 수 있다.

### 5.2 BMC와 VRP 사이의 제약 차이
BMC가 선택한 후보를 VRP가 모두 실제 시간 안에 처리하지 못할 수 있다.

따라서 BMC의 `cost`에는 단순 염화칼슘 비용만 넣기보다 다음을 포함하는 것이 좋다.

- 예상 작업 시간
- 대략적인 이동 시간 비용
- 차량 접근 난이도
- 환경 비용

MVP에서는 BMC로 후보를 고른 뒤 VRP에서 처리하지 못한 후보가 생기는지 확인하고, 이후에는 BMC 비용 계산에 이동 시간 추정치를 더한다.

---

## 6) 테스트 전략 보강

현재 테스트:

- `tests/test_dispatch_planner.py`
  - Greedy 수요 충족 검증
  - Knapsack 예산 내 최대 가치 선택 검증
  - BMC 중복 위험 cell 회피 검증
  - Hybrid BMC/Knapsack이 커버리지와 우선순위를 함께 반영하는지 검증
  - 선택 요약 비용/커버 cell 검증
  - VRP 용량/시간 제한 기반 경로 생성 검증
  - BMC 선정 후 VRP 경로화 통합 검증
- `tests/test_risk_scoring.py`
  - 추운 날씨/그림자 도로에서 위험도가 증가하는지 검증
  - 다중 기상 시나리오 가중 평균 위험도 계산 검증
  - 사고 확률이 유동성/사고 이력에 따라 증가하는지 검증
  - 결빙 위험도와 사고 확률에서 `DeicingCandidate` 생성까지 연결 검증
- `tests/test_torch_lightning_risk_model.py`
  - Lightning forward 출력 shape 및 0~1 범위 검증
  - 결빙 MLP와 사고확률 MLP가 같은 인터페이스로 동작하는지 검증
  - torch/lightning 미설치 시 `importorskip`
- `tests/test_deicing_models_smoke.py`
  - Pydantic 도메인 모델 생성 smoke test
  - `RiskPrediction`, `AccidentPrediction` 생성 smoke test

추가하면 좋은 테스트:

- 위험 예측:
  - `training_step` loss 계산 검증
  - 불균형 라벨에서 `pos_weight` 적용 검증
  - 결빙 확률 threshold별 recall/F1 계산 테스트
- 도메인 모델:
  - `target_time <= predicted_at`이면 validation 실패
  - 좌표 범위, bbox 순서, 작업 시간 순서 validation 실패
- 최적화:
  - priority 변환 규칙 테스트
  - 비용이 높은 차량-작업 조합 회피 테스트
  - 차량 총 용량이 수요보다 부족할 때 고위험 작업 우선 배정 테스트
  - 음수 effective cost 또는 우선순위 가중치 경계 테스트
  - BMC 선택 후보 중 VRP에서 미처리되는 후보 감지 테스트
  - 이동 시간 추정치를 포함한 candidate cost 비교 테스트

---

## 7) 평가 지표

결빙 예측은 사고 예방 시스템이므로 accuracy만으로는 부족하다.

필수 지표:

- Recall: 실제 위험 구간을 놓치지 않는지
- Precision: 불필요한 살포를 얼마나 줄이는지
- F1: precision/recall 균형
- ROC-AUC 또는 PR-AUC: threshold 변화에 따른 전체 성능
- High-risk recall: 위험도가 높은 구간을 얼마나 잘 잡는지
- Accident-risk recall: 사고 확률 상위 구간을 얼마나 놓치지 않는지

운영 지표:

- 염화칼슘 사용량 감소율
- 총 작업 비용 감소율
- 고위험 구간 미처리율
- 고사고확률 구간 커버율
- 사고/낙상 위험 감소 추정치
- 작업 완료 시간

---

## 8) 구현 로드맵

### 1단계: 현재 MVP 정리
- 현재 MLP + Lightning 모델 유지
- `risk_scoring.py`로 pseudo-risk/pseudo-accident -> priority -> cost -> candidate 생성 흐름 정리
- `RiskPrediction`, `AccidentPrediction`, `DeicingOperation`을 이용해 예측 -> 작업 생성 흐름 정리
- priority 변환 규칙 문서화
- 테스트 의존성 설치 후 전체 테스트 실행

### 2단계: 노션 feature 반영
- 그림자 지수, 유동 인구, grid cell 필드 유지 및 실제 데이터 매핑
- 겨울철 사고 통계/노면상태별 사고 데이터를 도로 또는 grid 단위로 매핑
- 환경 비용을 `estimate_deicing_cost_won`의 비용 항목으로 구체화
- Unity 50x50 grid 시뮬레이션 결과를 학습 데이터 포맷으로 변환

### 3단계: 공간 최적화 확장
- Knapsack baseline과 BMC 결과 비교
- Hybrid BMC/Knapsack으로 선택된 후보를 VRP로 차량별 경로화
- 이동 시간, 차량 위치, 살포재 잔량, 작업 시간창 반영
- Greedy/Knapsack/BMC/VRP 결과를 비용/안전 효과 기준으로 비교

### 4단계: 고도화 모델
- CNN 기반 grid 위험도 heatmap
- GNN 기반 도로 연결성 위험 예측
- 시간 변화 반영을 위한 Temporal 모델 검토
- VRP를 CARP 또는 Arc Routing으로 확장해 도로 구간 자체를 작업 대상으로 처리

---

## 9) 최종 판단
현재 `src` 구현은 노션 작품 기획의 전체 범위를 완성한 상태는 아니지만, **MVP의 출발점으로는 구조가 좋다**.

다만 발표/기획서 관점에서는 현재 모델을 “완성형 AI”처럼 쓰기보다, 아래처럼 표현하는 것이 설득력 있다.

> 현재 구현은 결빙 위험 예측과 사고 확률 예측을 AI 모델로 수행하고, 그 결과를 예산 기반 제설 대상 선정과 차량별 경로화에 연결하는 MVP이며, 최종 시스템은 grid/graph 기반 위험도 예측과 BMC + VRP/CARP 최적화를 결합하는 방향으로 확장한다.

이렇게 정리하면 현재 코드와 노션 기획 사이의 차이를 약점이 아니라 **단계적 개발 계획**으로 설명할 수 있다.

다운받은 프로젝트와 비교하면, 현재 프로젝트는 제출 산출물보다 코드 모듈화와 확장성에 초점이 있다. 따라서 실제 데이터 파이프라인은 다운로드 프로젝트의 방식을 참고하고, 이 프로젝트의 `risk_scoring.py`, `deicing_models.py`, `dispatch_planner.py`를 기준으로 기능을 분리해 발전시키는 방향이 좋다.
