# 모델 기획서

## 1) 요구사항 반영 요약
이번 버전은 요청사항에 맞춰 아래 4가지를 중심으로 설계했습니다.

- **PyTorch 기반 위험 예측 모델**
- **PyTorch Lightning 기반 학습/검증 루프 표준화**
- **MCMF(Min-Cost Max-Flow) 배차 최적화**
- **Greedy 배차 베이스라인**

---

## 2) 모델 아키텍처

### 2.1 결빙 위험 예측 (PyTorch + Lightning)
- 파일: `src/torch_lightning_risk_model.py`
- 구성:
  - `IcingRiskNet`: MLP(Linear-ReLU-Dropout x2 + Linear)
  - `IcingRiskLightningModule`:
    - 손실함수: `binary_cross_entropy_with_logits`
    - 지표: accuracy
    - optimizer: AdamW
- 입력 예시 특성:
  - 기온, 노면온도, 이슬점, 습도, 강수량, 풍속, 최근 살포량 등
- 출력:
  - 도로 세그먼트별 결빙 확률(0~1)

### 2.2 배차 최적화 (MCMF)
- 파일: `src/dispatch_planner.py`
- 그래프 모델링:
  - `source -> vehicle(capacity)`
  - `vehicle -> job(cost)`
  - `job -> sink(demand)`
- 목적:
  - 수요 충족을 최대화하면서 비용 최소화
  - job priority를 비용 함수에 반영(우선순위 높을수록 유리)

### 2.3 베이스라인 (Greedy)
- 파일: `src/dispatch_planner.py`
- 전략:
  - 높은 priority 작업부터, 용량이 큰 차량을 우선 배정
- 장점:
  - 매우 빠르고 구현 단순
- 한계:
  - 전역 최적 보장 없음(비용 효율은 MCMF보다 낮을 수 있음)

---

## 3) 도메인 모델과의 연결
- 기존 상세 도메인 모델(`src/deicing_models.py`)에서
  - `RiskPrediction`은 PyTorch 모델 출력값 저장
  - `DeicingOperation`은 MCMF/Greedy 계획 결과와 연동
- 운영 단계에서는
  1) 위험 예측 생성
  2) MCMF 계획 생성
  3) Greedy 결과와 비교(백테스트/폴백)
  4) 최종 작업 지시 반영

---

## 4) 테스트 전략
- `tests/test_dispatch_planner.py`
  - Greedy 수요 충족/우선순위 할당 검증
  - MCMF 비용 기반 할당 검증
- `tests/test_torch_lightning_risk_model.py`
  - Lightning 모듈 forward 출력 shape/범위 검증
  - 환경에 torch/lightning 미설치 시 `importorskip` 처리
